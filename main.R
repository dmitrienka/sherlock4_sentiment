library(twitteR)
library(tm)
library(SnowballC)
library(MASS)
library(wordcloud)

# setup with the twitter app keys:
#setup_twitter_oauth(consumer_key, consumer_secret, token, token_secret)
source("./setup.R")

# search
